pipeline {
    agent any
    environment {
    PROJECT_DIR = "${WORKSPACE}@tmp"
  }

    
    stages {
        stage('Run ETL Step by Step') {
            steps {
                bat 'jenkins\\run_job.bat'
            }
        }
    }
}
